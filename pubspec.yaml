name: silver_suite
description: "Silver Suite — Big buttons. Clear text. Real help. A senior-friendly utility kit."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.2.1
  shared_preferences: ^2.5.0
  equatable: ^2.0.7
  intl: ^0.19.0
  url_launcher: ^6.3.1
  torch_light: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
